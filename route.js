import OpenAI from "openai"; export async function POST(req){try{const {idea}=await req.json(); if(!process.env.OPENAI_API_KEY) return Response.json({error:"Ajoute OPENAI_API_KEY dans .env.local"},{status:500}); const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY}); const r=await client.responses.create({model:"gpt-5-mini",instructions:"Tu es le community manager de Dr Cloud. Écris en français, ton commercial naturel, direct et premium. Produis un contenu social prêt à publier. N'invente pas de prix ni de caractéristiques produit absents de la demande.",input:idea}); return Response.json({text:r.output_text})}catch(e){return Response.json({error:"Erreur IA : "+e.message},{status:500})}}